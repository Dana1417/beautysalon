from django.urls import path
from . import views

app_name = "bookings"

urlpatterns = [

    # ============================================================
    # 1) اختيار الخدمة
    # ============================================================
    path("services/", views.select_service_view, name="select_service"),

    # ============================================================
    # 2) اختيار الموظفة (اختياري)
    # ============================================================
    path("staff/", views.select_staff_view, name="select_staff"),

    # ============================================================
    # 3) اختيار التاريخ والوقت
    # ============================================================
    path("date-time/", views.select_date_time_view, name="select_date_time"),

    # ============================================================
    # 4) إدخال بيانات العميل
    # ============================================================
    path("customer-info/", views.customer_info_view, name="customer_info"),

    # ============================================================
    # 5) صفحة التأكيد قبل إرسال الحجز
    # ============================================================
    path("confirm/", views.confirm_booking_view, name="confirm_booking"),

    # ============================================================
    # 6) تنفيذ الحجز النهائي
    # ============================================================
    path("complete/", views.complete_booking_view, name="complete_booking"),

    # ============================================================
    # 7) صفحة نجاح الحجز
    # ============================================================
    path("success/", views.booking_success_view, name="booking_success"),

    # ============================================================
    # 8) صفحة حجوزات العميل
    # ============================================================
    path("my-bookings/", views.my_bookings_view, name="my_bookings"),
]
